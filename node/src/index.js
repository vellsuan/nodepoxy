//TODO:如果代码中有该标识，说明在标识处有功能代码待编写，待实现的功能在说明中会简略说明。

//FIXME:如果代码中有该标识，说明标识处代码需要修正，甚至代码是错误的，不能工作，需要修复，如何修正会在说明中简略说明。

function jkas() {
    let kl = 1111111111111111111111111
}

/**
 *
 * @param {number} str
 * @returns {number}
 */
//Shift + F6：重命名变量 引用他的都会被改变
function fun(str) {
    jkas();
    //将某段代码提取程一个fun crt alt m
   return 123
}


const number = 12313;
//提取变量 crt alt v将某段代码转为变量

let aa= number;
let jk=fun(123);
 console.log(jk);